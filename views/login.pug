extends main.pug
block content
  .container.mt-5
    .row.justify-content-center
      .col-md-6
        h2.mb-3 Logowanie
        form
          .mb-3
            label.form-label(for='username') Nazwa użytkownika:
            input.form-control(type='text' id='username')
          .mb-3
            label.form-label(for='password') Hasło:
            input.form-control(type='password' id='password')
          .mt-3.text-danger#error
          .mt-3.d-grid
            button.btn.btn-warning(type='submit' click='login()') Zaloguj się

  script(src='js/login.js')
